# IRFC 网站 SEO 全站排查报告
**日期**：2026-09-09 ｜ **站点**：https://irfc.com （Astro 6.4.7 静态站，50 页）｜ **范围**：页面加载 / 关键词 / 内链 / 内容 / 标签 / 结构化数据

---

## 一、已做对的地方（ strengths — 不要动）
- ✅ **每页唯一且含关键词的 `<title>`**（如 "In Vitro Fertilization (IVF) | INCINTA"），且全部显式设置，无页面回退到品牌默认描述。
- ✅ **每页恰好一个 `<h1>`**，无缺失、无重复。
- ✅ **全站 canonical / Open Graph / Twitter Card** 齐全（`Layout.astro` 统一生成）。
- ✅ **结构化数据扎实**（医疗 YMYL 站很关键）：
  - `MedicalOrganization`（全站，含地址/电话/服务语言/认证）
  - `Physician`（team 各页）
  - `MedicalClinic` + `LocalBusiness` + `parentOrganization`（locations.astro，分诊所本地 SEO）
  - `FAQPage`（faq.astro）
- ✅ **sitemap + robots.txt** 配置正确，robots 指向 `sitemap-index.xml`；另有 `llms.txt`（GEO/AI 搜索友好）。
- ✅ **图片以 webp 为主 + 描述性 alt**（如 "IRFC embryo transfer suite"）；装饰图用 `alt=""`+`aria-hidden`。
- ✅ **零外部 JS/CSS 包**（Tailwind 内联、脚本 `is:inline`）→ 无渲染阻塞请求，TTFB/首绘快。
- ✅ 安全响应头（`_headers`：X-Frame-Options 等）、移动端菜单与响应式完整。

---

## 二、🔴 必须修（影响排名 / 事实错误）
| # | 问题 | 位置 | 影响 |
|---|---|---|---|
| 1 | **首页诊所数量仍写 "5"**，与全站已改的 4 家矛盾 | `src/pages/index.astro:9`（"5 Southern California Clinics"）、`:20`（description "5 locations."） | 事实错误 + E-E-A-T 一致性崩坏；locations/services/standards 已写 4 家，仅首页错。**上次修 standards.ts 时漏了首页。** |
| 2 | **首页 Hero 视频 17.1 MB，`preload="auto"` + 自动播放** | `public/videos/brand-promo.mp4`；`index.astro:26` | 全站最高权重页的 LCP/INP 最大风险。移动端需缓冲完首帧才绘制，严重拖慢 Core Web Vitals（CWV 直接影响排名）。 |

**修复建议**
- #1：首页 stats 与 description 改 `4` / `four`，与 locations/services/standards 对齐。
- #2：视频压到 2–3 MB 内（H.264 + 更低码率，或额外提供 webm）；`preload="auto"`→`preload="none"`（poster 已设 `lobby-wide.webp` 作首帧兜底）；考虑用 `<link rel="preload" as="image" href="poster">` 保证 LCP 快速呈现。

---

## 三、🟡 建议修（重要优化）
| # | 问题 | 位置 | 建议 |
|---|---|---|---|
| 3 | **sitemap 收录了 noindex 页** | `dist/sitemap-0.xml` 含 `/contact/success/`（该页 `noindex`） | 在 `astro.config.mjs` 的 `sitemap()` 加 `filter: (p) => !p.includes('success')`，避免 "sitemap 要索引 / meta 要屏蔽" 冲突。 |
| 4 | **图片未优化 / 缺尺寸属性（CLS）** | `socal-network-map.png` & `southern-california-irfc-map-label-free.png` 各 **1.09 MB**；`dna-art.jpg` 385 KB；`team-rfc-wide.webp` 590 KB；多数 `<img>` 无 `width`/`height` | 把 PNG/JPG 转 AVIF/WebP 并压尺寸（地图图尤其）；给 `<img>` 补 `width`/`height`（或用 Astro `<Image>`）以稳 CLS。CSS `aspect-ratio` 已兜底大部分，但原始图仍过重。 |
| 5 | **首页 H1 无商业关键词**（标题/H1 错位） | `index.astro:32` H1 = "Life Is the Ultimate Art."，而 title 是 "Southern California IVF & Fertility Specialists" | 首页权重最高却用纯品牌标语作 H1。建议 H1 含主词，如 "Southern California IVF & Fertility Care" 下挂品牌副标。 |
| 6 | **若干标题偏泛、缺关键词** | `Services` / `Locations` / `Lab Technology`(technology) / `Patient Journey` / `Patient Resources` / `About IRFC` / `FAQ` | 改为 "Fertility Treatments & Services"、"IRFC Clinic Locations in Southern California"、"Fertility Lab & Embryology Technology" 等。 |
| 7 | **缺 BreadcrumbList + 评价聚合结构化数据** | 全站无 `BreadcrumbList`；`testimonials.astro` 有 39 条真实评价但无 `Review`/`AggregateRating` schema | 加面包屑 schema（SERP 富摘要）；testimonials 加 `AggregateRating`（Google/Yelp 评分）→ 星级富结果，CTR 提升明显。 |
| 8 | **/patient-portal 基本是孤页** | 仅 `appointment.astro:165` 一处链入，主导航/页脚均未链 | 若需索引，应在页脚或导航补充入口；否则可 `noindex`（它本质是外链 eIVF 门户跳转页）。 |

---

## 四、⚪ 可选 / 战略级
| # | 问题 | 说明 |
|---|---|---|
| 9 | **i18n 配置声明 5 语言但无对应内容 / 无 hreflang** | `astro.config.mjs` 声明 `locales:['en','zh','ja','ko','es']`；`orgSchema` 也声称 5 语言。但全站仅英文页，无 `hreflang` 标签。要么真做多语言（大工程）+ hreflang，要么移除 i18n 配置避免误导。当前无 hreflang 不报错，但错失多语言搜索流量。 |
| 10 | **无博客 / 长尾内容中枢** | 医疗 SEO 靠主题权威。现仅有薄薄的 `patient-resources` 枢纽，无文章。建议开 `/blog` 写 PCOS/年龄/反复流产等深度长尾文，内链回服务/病症页。 |
| 11 | **trailing-slash 不一致** | canonical 为 `/services/`（带斜杠），内链多为 `/services`（无斜杠）。同文件内容一致，危害低；可统一 `trailingSlash` 策略消除歧义。 |
| 12 | **轻微 PWA/UX 缺失** | 无 `apple-touch-icon`、`manifest.webmanifest`、`theme-color` meta。对排名无直接影响，影响 iOS 书签体验。 |
| 13 | **潜在关键词自相蚕食** | `About IRFC` / `Why Choose IRFC` / `Our Team` 三页意图相近；`services.astro` 与各服务页。当前区分尚可，后续加内容时注意聚焦。 |
| 14 | **og:image 尺寸硬编码 1200×630** | `Layout.astro` 写死；默认图 `lobby-wide.webp` 未必恰为该比例，社交卡片可能裁切。核对实际尺寸即可。 |

---

## 五、性能实测数据（dist 构建产物）
- **总产物 31 MB**（17 MB 视频占大头）。
- **Hero 视频**：`brand-promo.mp4` = **17.1 MB**（🔴 头号性能问题）。
- **最重图片**：`socal-network-map.png` 1.09 MB、`team-rfc-wide.webp` 590 KB、`dna-art.jpg` 385 KB、`couple-clinic.webp` 323 KB。
- **外部 JS/CSS 包 = 0**（全部内联）→ 渲染路径干净，这是优势。
- **最大 HTML 页**：testimonials 129 KB（39 条评价）、faq 92 KB、outcomes 77 KB —— 体量正常。
- **sitemap**：51 条 URL，含应排除的 `/contact/success/`。

---

## 六、优先级行动清单（建议顺序）
1. 🔴 #1 首页诊所数改 4（5 分钟，消除事实错误）
2. 🔴 #2 压缩/降载 Hero 视频（对 CWV 与首页排名影响最大）
3. 🟡 #3 sitemap 排除 noindex 页
4. 🟡 #4 图片转 AVIF/WebP + 补尺寸
5. 🟡 #5/#6 首页 H1 与泛化标题补关键词
6. 🟡 #7 加 BreadcrumbList + 评价聚合 schema
7. ⚪ #9–#14 按战略节奏推进

> 注：本次为纯排查，未改动任何代码。如需我直接落地修复，回复「修 🔴」或「全部修复」即可。
