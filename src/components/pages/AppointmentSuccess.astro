---
// 預約成功頁（英文 / 繁體中文共用元件）
// 由原 /contact/success 遷移而來：表單提交後落地頁，並由本頁加載的 GTM 觸發 generate_lead。
import Layout from '../../layouts/Layout.astro';
import Nav from '../../components/Nav.astro';
import Footer from '../../components/Footer.astro';
import { getLocaleFromPath } from '../../i18n';
import { BRAND_LANGUAGE } from '../../config/brand';
import MainPhone from '../MainPhone.astro';

const locale = getLocaleFromPath(Astro.url.pathname);
const ZH = locale === 'zh-hant';
---

<Layout
  title={ZH ? '已收到您的預約' : 'Request Received'}
  description={ZH ? '我們已收到您的諮詢請求，IRFC 照護團隊將於一個工作天內與您聯繫。' : 'Your consultation request has been received. A member of the IRFC care team will contact you within one business day.'}
  noindex
>
  <Nav />

  <section class="lotus-watermark pt-40 pb-28" style="background: var(--color-pearl-50);">
    <div class="max-w-3xl mx-auto px-6 lg:px-10 text-center">

      <div class="flex justify-center mb-8">
        <div
          class="w-16 h-16 rounded-full flex items-center justify-center"
          style="background: var(--color-amethyst-600);"
        >
          <svg class="w-8 h-8 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
            <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
          </svg>
        </div>
      </div>

      <p class="text-xs tracking-widest uppercase mb-5" style="color: var(--color-champagne-600); font-family: var(--font-sans);">
        {ZH ? '已收到請求' : 'Request Received'}
      </p>

      <h1
        style="font-family: var(--font-serif);"
        class="text-4xl md:text-5xl font-light text-stone-800 mb-6 leading-tight"
      >
        {ZH ? <>謝謝您。<br /><em class="not-italic" style="color: var(--color-champagne-600);">您的旅程自此開始。</em></> : <>Thank you.<br /><em class="not-italic" style="color: var(--color-champagne-600);">Your beginning starts here.</em></>}
      </h1>

      <p class="text-lg text-stone-500 font-light leading-relaxed mb-4 max-w-xl mx-auto">
        {ZH ? '我們的照護團隊將於一個工作天內與您聯繫，為您安排諮詢。' : 'A member of our care team will reach out within one business day to schedule your consultation.'}
      </p>
      <p class="text-sm text-stone-400 font-light leading-relaxed mb-12 max-w-xl mx-auto">
        {ZH ? BRAND_LANGUAGE.promise.zhHant : BRAND_LANGUAGE.promise.en}
      </p>

      <div class="flex flex-col sm:flex-row items-center justify-center gap-4 mb-12">
        <MainPhone class="btn-primary" />
        <a href="/locations" class="btn-outline">
          {ZH ? '查看診所地點' : 'View our locations'}
        </a>
      </div>

      <div class="pt-10" style="border-top: 1px solid var(--color-pearl-300);">
        <p class="text-sm text-stone-500 font-light mb-2">
          {ZH ? '需要更快聯絡我們？' : 'Need to speak with someone sooner?'}
        </p>
        <p class="text-sm text-stone-400 font-light">
          {ZH ? '我們於一般營業時間提供總機服務，照護團隊將協助您安排合適的諮詢時間。' : 'Our main line is open during regular business hours — a care coordinator will help you schedule the right consultation.'}
        </p>
      </div>

    </div>
  </section>

  <Footer />
</Layout>

<script>
  // 仅在“真实提交到达”时打点（URL 带 ?lead=1，由表单提交成功后跳转带入）。
  // 成功页加载了 GTM，gtag 由 GA4 Configuration 标签初始化；待其就绪后发 generate_lead。
  // 发完后用 history.replaceState 去掉 ?lead=1，避免用户刷新本页造成重复计数。
  (function () {
    function fireLead() {
      if (new URLSearchParams(location.search).get('lead') !== '1') return;
      var attempts = 0;
      var tryFire = function () {
        if (window.gtag) {
          window.gtag('event', 'generate_lead');
          if (history.replaceState) {
            history.replaceState(null, '', location.pathname + location.hash);
          }
          return;
        }
        if (attempts++ < 20) setTimeout(tryFire, 200);
      };
      tryFire();
    }
    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', fireLead);
    } else {
      fireLead();
    }
  })();
</script>
