# IRFC 全站 SEO 审计报告

**日期**：2026-09-15 ｜ **站点**：https://irfc.com （Astro 6.4.7 静态站，107 个已构建页面）
**范围**：① 关键词部署 ② 内部链接 ③ 死链接 ④ 无效/无用内容清理 ⑤ 其他 SEO 问题
**对照基准**：2026-09-09 旧版审计（已逐项复核修复状态）

> 方法说明：本次同时做了**源码级**与**渲染级**双重验证。重新 `astro build` 生成新鲜 `dist`（107 页），用脚本爬取全部 **14,102** 个内部链接核对目标存在性；并逐页追查 `title` / `description` / `h1` 的真实来源（数据文件 `standards.ts` / `team.ts` / `blog.ts` + 共享组件 `StandardsHub` / `ServicesHub` / `ConditionLayout` / `TeamMember` 等，封装页本身不写 frontmatter）。

---

## 〇、总览（一页结论）

| 维度 | 结论 | 严重度 |
|---|---|---|
| ① 关键词部署 | 服务/病症/博客/多数枢纽页优秀；首页 H1 缺商业词、6 个标准枢纽标题抽象、少量工具页标题偏泛 | 🟡 中 |
| ② 内部链接 | 链接图健康，无重复导航、无锚点失效 | 🟢 良好 |
| ③ 死链接 | 渲染爬取 **0 死链**（14,102 链接全命中） | 🟢 良好 |
| ④ 无用内容清理 | 无 demo/空壳/薄页；但存在 3 处**内容正确性 BUG**（NAP 电话、邮箱域名、面包屑死 URL） | 🟡 中 |
| ⑤ 其他 SEO | 视频仍是头号性能风险；结构化数据/图片/hreflang/sitemap 均已达标 | 🔴 1 项高 |

**对比上次审计已修复 ✅**：首页诊所数 5→4、sitemap 排除 noindex 页、hreflang 正确互链、BreadcrumbList 生成、i18n 收敛 en+zh-hant、评价聚合 `AggregateRating`+`FAQPage` schema 补齐、图片已压缩（最大栅格图 <0.23MB）、重复导航入口修复、WhatsApp 笔误修正。

---

## 一、① 关键词部署审计

### 强项（保持）
- **服务页**（`/services/ivf`、`/iui`、`/egg-freezing`、`/pgt`、`/icsi`、`/recurrent-pregnancy-loss`、`/donors-surrogacy`、`/lgbtqia`）：`title` 与 `h1` 均为全称术语（如 "In Vitro Fertilization (IVF)"），关键词密度高。
- **病症页**（6 个 `conditions/*`）：`h1` = 病症名（PCOS / Endometriosis 等），`title` 带 "fertility at IRFC" 修饰，精准。
- **博客**（3 篇）：`title` 为问句式长尾（"IVF Success Rates: How to Read Them…"），正文含 `h2` 小标题 + 内链，符合主题权威建设。
- **首页 `title`**："Incinta Reproductive Fertility Center — IVF & Fertility Care in Southern California"，含核心词。
- 全站 **无重复 title、无缺 h1、无 h1 重复**；均自定义 `description`（无回退默认）。

### 待修问题

| # | 页面 / 类型 | 问题 | 证据 |
|---|---|---|---|
| **K1** | 首页 `h1` | `h1` = "Life Is the Ultimate Art."（纯品牌标语），**完全无 IVF/fertility 商业词**；而 `title` 含商业词 → H1 与 title 意图错位。最高权重页的 H1 未承载搜索意图（正文已补 IVF/IUI 关键词，部分缓解） | `Home.astro:53-56`（h1）vs `:32-34`（title） |
| **K2** | 6 个标准枢纽页 | `title`/`h1` 抽象、零商业词："Collective expertise" / "Precision & personalization" / "Laboratory excellence" / "Privacy & transparency" / "The art of care" / "Exceptional care, within reach"。这些是主导航一级入口、权重不低，却错失 "fertility care / IVF / clinic" 搜索 | `standards.ts`（各 `title`）→ `StandardsHub.astro:20,40` |
| **K3** | `/locations` | `title` = "Locations"，**无地域词**，本地搜索（"fertility clinic near me"）竞争力弱 | `Locations.astro:87` |
| **K4** | `/about`、`/why-irfc`、`/contact`、`/patient-journey` | 标题偏泛（"About IRFC" / "Why Choose IRFC" / "Contact" / "Patient Journey"），可补地域或服务词 | `About.astro:42` / `WhyIrfc.astro:70` / `Contact.astro:13` / `PatientJourney.astro:190` |
| **K5** | `description` 长度 | 全站均有自定义描述（好）；但首页 EN ≈140 字符、标准枢纽 `description` 偏长，部分接近 160 上限，需确保移动端不被截断 | `Layout.astro:33` 默认值 + 各组件 `description` |

**关键词自相蚕食（低风险）**：`About` / `Why IRFC` / `Team` 三页意图相近但区分尚可；`ServicesHub` 与 8 个服务详情页为"概览↔详情"关系，正常。当前无需合并。

**修复建议**
- K1：首页 H1 改为含主词，如 "Southern California IVF & Fertility Care"，品牌标语降为副标或视觉化处理。
- K2：标准枢纽 `title` 追加场景词，如 "Collective Expertise in Fertility Care" / "Laboratory Excellence in IVF"；`h1` 同步。
- K3：`/locations` → "IRFC Fertility Clinic Locations in Southern California"。
- K4：工具页标题加地域/动作词（如 "Contact IRFC — Southern California Fertility Clinics"）。

---

## 二、② 内部链接审计

- **链接图健康**：全局导航（`Nav`+`standards.ts`）、页脚（`Footer`）、浮动联系、页面正文 CTA 全覆盖；去重后内部目标 ≈60 个英文基准路由 + 繁中镜像，与目标页面一一对应。
- **无同菜单重复目标**：旧审计发现的 "Visit Us 内 Schedule a Consultation 与 Contact Us 同指向 /contact" 已修复（现分别为 `/appointment` / `/contact` / `/consultation` / `/locations`）。
- **锚点链接全部有效**：`/services#donors`、`/services#lgbtqia`、`/appointment#request`、`/outcomes#credentials` 的 `id` 均存在。
- **无 noindex 页被内链或收进 sitemap**（仅 `/contact/success` 为 noindex，且 sitemap 已排除、无入链）。
- **繁中镜像**通过 `localeHref()` 在同语言内互链，跨语言由 `hreflang` 切换器承接（非断链、非孤儿）。

---

## 三、③ 死链接审计（渲染级验证）

对新鲜 `dist`（107 页）爬取全部内部链接：

- **检查内部链接数**：14,102
- **真实死链（404）**：**0** ✅
- **孤儿页（无入链）**：仅 `/contact/success`（预期内的表单提交目的地，已 `noindex` + sitemap 排除）——属正确设计，非问题。英文侧 0 孤儿。
- **模板表达式**（如 `/standards/${slug}`、`/blog/${slug}`、`/team/${slug}`）构建后均展开为真实路由，非错误。
- `favicon.svg` 等静态资源已存在，不计为路由死链。

> 结论：全站**无死链、无真孤儿页**。与源码级链接审计结论一致，双重验证通过。

---

## 四、④ 无效 / 无用内容清理

### 已确认无需清理（此前已处理）
- ✅ **`/demo/*` 评审稿页已移除**：`src/pages` 下无 `demo` 目录，不再被部署。
- ✅ **`/patient-portal` 非空壳**：内容完整（门户介绍、登录 CTA、新病人引导、功能清单、FAQ、8 个教学视频、App 下载）。仅 "Register Online" 暂指向登录（eIVF 自助注册未开通，代码注释已说明），属已知占位行为，非死页。
- ✅ **`/contact/success`** 正确处理（noindex + sitemap 排除 + 无入链）。
- ✅ 全站未出现 `example.com` / `lorem` / `staging` 等占位痕迹。

### ⚠️ 需清理 / 修正的内容正确性 BUG

| # | 问题 | 位置 | 影响 | 建议 |
|---|---|---|---|---|
| **C1** | **NAP 电话不一致**：页脚把 4 家诊所**全部**写成主号 `(424) 432-4732`；而 `Locations` / `Appointment` / `Consultation` 用各城市直拨号（Torrance `424-212-4087`、Beverly Hills `213-600-8088`、Corona `951-272-2221`、Irvine `949-453-8600`） | `Footer.astro`（locations 数组）vs `Locations.astro:25-30` 等 | 本地 SEO / Google 商家资料 NAP 一致性受损，可能被判信号冲突 | 以各城市直拨号为单一事实来源，同步修正页脚（或反之统一主号，需你定夺"主号"归属） |
| **C2** | **邮箱域名不一致**：门户页写 `Support@RFCfamily.com`，其余全站为 `info@irfc.com` | `PatientPortal.astro:370` vs `Footer.astro:65` 等 | 若门户支持邮箱本就归属 rfcfamily.com 则正确；否则应统一为 `info@irfc.com` | 确认门户支持邮箱是否应走 `rfcfamily.com` 域名；若否，改为 `info@irfc.com` |
| **C3** | **面包屑结构数据死 URL**：`BreadcrumbList` 为 `/standards/*`、`/conditions/*` 生成父级面包屑，其 `item` 指向**不存在的** `/standards/`、`/conditions/` 索引页 | `Layout.astro:96-110` | 结构化数据中出现 404 URL，属轻微 schema 错误（不影响渲染） | 方案 A：新增 `/standards`、`/conditions` 索引 hub 页（推荐，顺带补内部链接中枢）；方案 B：面包屑跳过该父级或指向真实页 |

> 说明：当前**没有**真正意义上的"垃圾内容"可删除；清理重点应放在上述 3 处**内容正确性**修正上。

---

## 五、⑤ 其他 SEO 问题

### 🔴 高优先级
| # | 问题 | 位置 | 说明 |
|---|---|---|---|
| **P1** | **Hero 视频仍是头号性能/CWV 风险** | `public/videos/brand-promo.mp4` = **15.59 MB**、`brand-promo.webm` = 7.03 MB；`Home.astro:45` `preload="auto"` + `autoplay muted loop` | 最高权重页（首页）的 LCP/INP 最大拖累。旧审计 #2 未修复。建议：压到 2–3 MB（H.264 + 更低码率，或额外提供 webm）、`preload="none"`、`poster`（`lobby-wide.webp`）作 LCP 兜底 |

### 🟢 已达标（保持）
- **零外部 JS/CSS 包**（Tailwind 内联、脚本 `is:inline`）→ 无渲染阻塞，TTFB/首绘快。
- **图片已优化**：当前最大栅格图仅 0.23 MB（`network-comfort-irvine.jpg`）；旧审计的 1.09 MB 地图图已压缩/替换。旧 #4 基本解决。多数 `<img>` 已带 `width`/`height`。
- **结构化数据扎实**（YMYL 医疗站关键）：`MedicalOrganization`（全站）、`Physician`（team 各页）、`MedicalClinic`+`LocalBusiness`+`parentOrganization`（locations）、`FAQPage`（faq）、`Article`（blog）、`AggregateRating`+`Review`（testimonials）。旧 #7 已补齐。
- **hreflang** `en` / `zh-hant` / `x-default` 正确互链，且仅在繁体版存在时输出 `zh-hant`（避免错误 alternate）。旧 #9 已修复。
- **canonical** 按 pathname 生成，尾端斜线已统一（记忆中防回归规则生效）。
- **robots.txt** 指向 `sitemap-index.xml`；sitemap 含 106 URL（107 页 − `/contact/success`），分区为 `sitemap-index.xml` + `sitemap-0.xml`。旧 #3 已修复。

### 🟡 中低优先级
| # | 问题 | 位置 | 建议 |
|---|---|---|---|
| **P2** | `og:image` 尺寸写死 `1200×630` | `Layout.astro:142-143` | 核对默认图 `lobby-wide.webp` 实际比例，避免社交卡片裁切 |
| **P3** | 长尾内容中枢仍单薄 | 仅 3 篇博客 | 持续扩展 PCOS / 年龄 / 反复流产等深度长尾文，内链回服务/病症页（旧 #10） |
| **P4** | PWA/UX 缺失 | 无 `apple-touch-icon` / `manifest.webmanifest` / `theme-color` | 对排名无直接影响，影响 iOS 书签体验（旧 #12） |

---

## 六、优先级行动清单（建议顺序）

**P0 — 影响排名 / 本地 SEO**
1. **P1** 压缩 Hero 视频 + `preload="none"`（对首页 CWV 与排名影响最大）
2. **C1** 统一诊所电话数据源（消除 NAP 冲突）

**P1 — 关键词与结构**
3. **K1** 首页 H1 加商业关键词
4. **K2** 6 个标准枢纽页 `title`/`h1` 加场景词
5. **K3/K4** `/locations` 等工具页标题加地域/服务词
6. **C3** 修正面包屑死 URL（建议顺带新建 `/standards`、`/conditions` 索引 hub）

**P2 — 收尾**
7. **C2** 确认并统一门户支持邮箱域名
8. **P2** 核对 `og:image` 比例
9. **P3** 持续博客长尾内容
10. **P4** 补 PWA/UX 资源

---

## 七、本次未改动说明
本次为**纯审计 + 渲染验证**，未修改任何源码或内容；临时爬取脚本与构建日志已清理，`dist` 已刷新为最新 107 页构建产物。如需我直接落地上述修复（尤其是 P1 视频、K1/K2/K3 标题、C1 电话、C3 面包屑），回复"修 P0"或指定条目即可。
